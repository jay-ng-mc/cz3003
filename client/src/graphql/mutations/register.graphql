mutation Register( $email: String!, $username: String!, $password: String!, $password2: String!, $userRole: String!) {
    register(options: { email: $email, username: $username, password: $password, password2: $password2, userRole: $userRole }){
        errors{
          field
          message
        }
        user{
          ...RegularUser
        }
      }
    }